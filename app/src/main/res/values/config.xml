<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Taplink SDK Configuration -->
    <!-- Note: These are sample values, please replace with real configuration when using -->

    <!-- Application identifier - Assigned by Taplink platform -->

    <!-- Merchant identifier - Assigned by Taplink platform -->

    <!-- Secret key - Used for signature verification, assigned by Taplink platform -->

    <!-- Default connection mode: APP_TO_APP, CABLE, LAN, CLOUD -->

    <!-- Default currency type -->

    <!-- Connection timeout (milliseconds) -->

    <!-- Transaction timeout (milliseconds) -->

    <!-- Whether to enable debug mode -->

    <!-- LAN connection default configuration -->

    <!-- Cloud mode default configuration (UAT preset) -->

    <!-- Application version information -->

    <!-- Supported transaction types (for UI display control) -->

    <!-- Amount limit configuration -->

    <!-- Retry configuration -->
</resources>
